@using SYBD.Db.MongoDb.Models
@model (string Time, IEnumerable<Stock> Data)

@{
    ViewData["Title"] = "Stock list";
}
<div class="text-center">
    <div style="display: flex; justify-content: space-between;">
        <h4 class="display-4">Stocks <span style="color: #449d35">Mongo</span></h4>
        <h4 class="display-4">Time: <span style="color: #449d35">@(Model.Time)</span></h4>
    </div>
</div>
<div class="text-center" style="margin-top: 20px">
    @{
        <table class="table">
            <thead>
                <tr>
                    <th>
                        Id
                    </th>
                    <th>
                        Address
                    </th>
                    <th>
                        Photos
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Data)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Address)
                        </td>
                        <td>
                            @foreach (var photo in item.Photos)
                            {
                                <p>@(photo.Item1): @(photo.Item2)<br /></p>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
